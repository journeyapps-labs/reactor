---
'@journeyapps-labs/reactor-mod': minor
'@journeyapps-labs/lib-reactor-builder': minor
---

Add preferred entity handlers and an `EntityHandlerBank` for resolving a default handler.

Pass the builder's webpack instance to module webpack configuration functions, so custom configurations can share the compiler instance used for builds.
